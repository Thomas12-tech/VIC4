document.getElementById("checklistForm").addEventListener("submit", function (e) {
  e.preventDefault();
  alert("Checklist submitted successfully!");
  // Optionally, gather and send data via fetch/AJAX
});
